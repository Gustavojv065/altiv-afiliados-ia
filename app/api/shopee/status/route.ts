import {NextResponse} from "next/server";import {createClient} from "@/lib/supabase/server";export async function GET(){const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)return NextResponse.json({connected:false,authenticated:false},{status:401});const{data}=await supabase.from("affiliate_accounts").select("connection_status,last_sync_at").eq("user_id",user.id).eq("platform","shopee").maybeSingle();return NextResponse.json({authenticated:true,connected:data?.connection_status==="connected",lastSyncAt:data?.last_sync_at??null});}
