export type RadarInput={commissionRate?:number|null;rating?:number|null;salesCount?:number|null;discountRate?:number|null};export function calculateTrendScore(p:RadarInput){const commission=Math.min(Math.max(p.commissionRate??0,0),30)/30*35;const rating=Math.min(Math.max((p.rating??0)-3,0),2)/2*20;const sales=Math.min(Math.log10(Math.max(p.salesCount??0,1))/5,1)*30;const discount=Math.min(Math.max(p.discountRate??0,0),60)/60*15;return Math.round((commission+rating+sales+discount)*100)/100;}
