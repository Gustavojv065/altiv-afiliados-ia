name: Build ALTIV Android Beta 8
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "android/**"
      - ".github/workflows/build-android-v2.yml"
permissions:
  contents: write
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.10.2"
      - name: Build APK
        working-directory: android
        run: gradle assembleDebug
      - name: Prepare APK
        run: cp android/app/build/outputs/apk/debug/app-debug.apk ALTIV-Afiliados-IA-Beta8.apk
      - name: Publish Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release delete beta-8 --yes --cleanup-tag 2>/dev/null || true
          gh release create beta-8 ALTIV-Afiliados-IA-Beta8.apk --title "ALTIV Afiliados IA - Beta 8" --notes "Layout mobile responsivo: correção de status bar e navigation bar no Android 15+, medidas em dp, navegação inferior maior e conteúdo adaptado à tela."
